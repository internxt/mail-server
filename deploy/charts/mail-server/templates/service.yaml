apiVersion: v1
kind: Service
metadata:
  name: mail-server
  labels:
    app: mail-server
    owner: internxt
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mail-server
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
