apiVersion: v1
kind: Service
metadata:
  name: mail-api
  labels:
    app: mail-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mail-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
